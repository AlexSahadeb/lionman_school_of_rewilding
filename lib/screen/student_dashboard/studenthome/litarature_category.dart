import 'package:flutter/material.dart';
class LitaratureCategory extends StatelessWidget {
  const LitaratureCategory({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        body: Center(
          child: Text("Litarature category"),
        ),
      ),
    );
  }
}