import 'package:flutter/material.dart';
class ArtsCategory extends StatelessWidget {
  const ArtsCategory({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        body: Center(
          child: Text("arts category"),
        ),
      ),
    );
  }
}